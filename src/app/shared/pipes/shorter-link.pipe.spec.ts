import { ShorterLinkPipe } from './shorter-link.pipe';

describe('ShorterLinkPipe', () => {
  it('create an instance', () => {
    const pipe = new ShorterLinkPipe();
    expect(pipe).toBeTruthy();
  });
});
